language: scala
scala:
- 2.10.4
jdk:
- openjdk7
cache:
  directories:
  - $HOME/.ivy2/cache
script:
  - sbt -J-Xmx4G test
after_script:
  - mkdir -p shippable/testresults
  - cp */target/test-reports/*.xml shippable/testresults
